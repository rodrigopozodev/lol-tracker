{
  "name": "lol-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "node scripts/clean-next.js",
    "dev": "next dev -p 3000",
    "dev:turbo": "next dev -p 3000 --turbopack",
    "dev:poll": "cross-env WATCHPACK_POLLING=1 CHOKIDAR_USEPOLLING=1 next dev -p 3000",
    "dev:poll:turbo": "cross-env WATCHPACK_POLLING=1 CHOKIDAR_USEPOLLING=1 next dev -p 3000 --turbopack",
    "dev:webpack": "next dev -p 3000",
    "dev:webpack:poll": "cross-env WATCHPACK_POLLING=1 CHOKIDAR_USEPOLLING=1 next dev -p 3000",
    "dev:clean": "npm run clean && npm run dev",
    "dev:force": "node scripts/dev-force.js",
    "build": "next build",
    "start": "next start -p 3000",
    "start:force": "node -e \"require('child_process').execSync('netstat -ano | findstr :3000',{stdio:'ignore'}); try{const out=require('child_process').execSync('netstat -ano | findstr :3000',{encoding:'utf8'}); const pids=[...new Set(out.split(/\\r?\\n/).filter(l=>l.trim()).map(l=>l.trim().split(/\\s+/).pop()).filter(pid=>pid&&/^[0-9]+$/.test(pid)))]; for(const pid of pids){try{require('child_process').execSync('taskkill /PID '+pid+' /F',{stdio:'ignore'});}catch{}}}catch{}; require('child_process').spawn(process.platform==='win32'?'npm.cmd':'npm',['run','start'],{stdio:'inherit',shell:true});\"",
    "lint": "eslint"
  },
  "dependencies": {
    "lucide-react": "^0.546.0",
    "next": "15.5.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "cross-env": "^10.1.0",
    "eslint": "^9",
    "eslint-config-next": "15.5.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
